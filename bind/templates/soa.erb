;; begin SOA record
$TTL 1H
@		IN	SOA       ns1.<%= scope.lookupvar("bind::ns_domain") %>.  hostmaster.<%= scope.lookupvar("bind::ns_domain") %>.  (
                               <%= scope.lookupvar("bind::utime_serial") %>       ; Serial
                                8H             ; Refresh
                                2H             ; Retry
                                4W             ; Expire
                                1D )           ; Minimum

		IN	NS	ns1.<%= scope.lookupvar("bind::ns_domain") %>.
ns1		IN	A	<%= scope.lookupvar("bind::ip_domain") %>

;; end SOA record
